namespace OAuthServer.Endpoints;

public static class LoginEndpoint
{
    public static IResult GetHandler(HttpRequest request)
    {
        return Results.Ok();
    }

    public static IResult PostHandler(HttpRequest request)
    {
        return Results.Ok();
    }
}